info:
  name: TRIGGER_SETTLEMENT
  type: http
  seq: 1

http:
  method: POST
  url: "{{URL}}/system/scheduler/settlement/trigger"
  headers:
    - name: x-scheduler-api-key
      value: zelpay_scheduler_secret_2026_change_me
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
      "period_date" : "2026-06-27"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
