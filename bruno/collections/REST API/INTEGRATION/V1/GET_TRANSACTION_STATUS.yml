info:
  name: GET_TRANSACTION_STATUS
  type: http
  seq: 2

http:
  method: GET
  url: "{{URL}}/v1/integration/transactions/:id"
  headers:
    - name: x-api-key
      value: "{{API_KEY}}"
  params:
    - name: id
      value: C63AC661248F4F09B12E6F6ACA7ECA29
      type: path

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |
  # Get Transaction Status (Integration v1)

  Mengambil detail/status satu transaksi milik merchant. Auth API key; mode mengikuti
  environment key.
