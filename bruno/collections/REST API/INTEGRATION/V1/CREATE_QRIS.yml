info:
  name: CREATE_QRIS
  type: http
  seq: 1

http:
  method: POST
  url: "{{URL}}/v1/integration/transactions/qris"
  headers:
    - name: x-api-key
      value: zp_live_be001f58a222ff1bc8a2ce9032060d6b61b8a13712c080a0
  body:
    type: json
    data: |-
      {
        "amount": 100,
        "order_id": "OR2-2226-001241"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |
  # Create QRIS (Integration v1)

  Membuat transaksi QRIS untuk merchant. Auth memakai header `x-api-key`; mode mengikuti
  environment key (prefix `zp_test_` → sandbox, `zp_live_` → production).
