info:
  name: GET_MY_TRANSACTIONS
  type: http
  seq: 1

http:
  method: GET
  url: "{{URL}}/transactions"
  headers:
    - name: x-app-mode
      value: production
  params:
    - name: status
      value: SUCCESS
      type: query
      disabled: true
    - name: q
      value: "002"
      type: query
      disabled: true
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
