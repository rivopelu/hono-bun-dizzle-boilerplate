info:
  name: VALIDATE_TRANSACTION
  type: http
  seq: 4

http:
  method: POST
  url: "{{URL}}/transactions/:id/validate"
  headers:
    - name: x-app-mode
      value: "{{SANDBOX_MODE}}"
  params:
    - name: id
      value: 8ad9f6f14b9e4a07b28a8dbc5cb4561a
      type: path
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
