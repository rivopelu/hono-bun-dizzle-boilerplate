info:
  name: CANCEL_TRANSACTION
  type: http
  seq: 6

http:
  method: POST
  url: "{{URL}}/transactions/:id/cancel"
  headers:
    - name: x-app-mode
      value: production
  params:
    - name: id
      value: 29ec21bb4c0441ad94ff2cb6ce453127
      type: path
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
