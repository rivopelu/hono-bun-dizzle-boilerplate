info:
  name: ME
  type: http
  seq: 1

http:
  method: GET
  url: "{{URL}}/auth/me"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
