info:
  name: RESEND_OTP
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{URL}}/auth/resend-otp"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
