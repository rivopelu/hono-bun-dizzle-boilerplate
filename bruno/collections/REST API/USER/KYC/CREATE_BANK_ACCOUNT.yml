info:
  name: CREATE_BANK_ACCOUNT
  type: http
  seq: 4

http:
  method: POST
  url: "{{URL}}/bank-accounts"
  body:
    type: json
    data: |-
      {
          "payout_channel_id": "pch_id_disini",
          "bank_account_number": "1234567890",
          "bank_account_holder_name": "John Doe"
      }
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |
  # Create Bank Account (User POV)

  Menambahkan bank account baru untuk user.

  ## Headers
  - `Authorization: Bearer <TOKEN>` (JWT User)

  ## Body
  - `payout_channel_id` (string): ID payout channel (hanya BANK)
  - `bank_account_number` (string): Nomor rekening
  - `bank_account_holder_name` (string): Nama pemilik rekening
