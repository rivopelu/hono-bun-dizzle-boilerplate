info:
  name: UPLOAD_PRIVATE
  type: http
  seq: 1

http:
  method: POST
  url: "{{URL}}/storage/upload/private"
  body:
    type: multipart-form
    data:
      - name: file
        type: file
        value: []
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
