info:
  name: GET_ASSET_URL
  type: http
  seq: 3

http:
  method: GET
  url: "{{URL}}/storage/assets/:assetId/url"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
