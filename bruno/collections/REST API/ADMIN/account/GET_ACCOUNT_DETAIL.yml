info:
  name: GET_ACCOUNT_DETAIL
  type: http
  seq: 2

http:
  method: GET
  url: "{{URL}}/admin/accounts/:id"
  headers:
    - name: x-app-mode
      value: production
  params:
    - name: id
      value: f1a9927100384ba9a15a3dc6cc26b2fa
      type: path
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
