info:
  name: REGISTER
  type: http
  seq: 2

http:
  method: POST
  url: "{{URL}}/auth/register"
  body:
    type: json
    data: |-
      {
          "name": "rivo pelu",
          "email": "rivopelu12@gmail.com",
          "password": "@Rivopelu123",
          "confirm_password": "@Rivopelu123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
