info:
  name: GOOGLE_SIGN_IN
  type: http
  seq: 3

http:
  method: POST
  url: "{{URL}}/auth/google"
  body:
    type: json
    data: |-
      {
          "id_token": "eyJhbGciOiJSUzI1NiIsImtpZCI6...",
          "remember_me": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
