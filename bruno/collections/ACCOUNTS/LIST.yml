info:
  name: LIST
  type: http
  seq: 1

http:
  method: GET
  url: "{{URL}}/accounts?size=1&page=0"
  params:
    - name: size
      value: "1"
      type: query
    - name: page
      value: "0"
      type: query
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
