info:
  name: TEST MAIL
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/send-test-email"
  body:
    type: json
    data: |-
      {
        "target" : "rivopelu12@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
