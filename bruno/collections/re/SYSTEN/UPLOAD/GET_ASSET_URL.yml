info:
  name: GET ASSET URL
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/storage/assets/:assetId/url"
  auth:
    type: bearer
    token: "{{token}}"

params:
  path:
    assetId: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
