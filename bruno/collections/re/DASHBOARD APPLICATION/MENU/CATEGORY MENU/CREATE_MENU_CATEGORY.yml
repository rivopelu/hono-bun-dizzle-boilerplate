info:
  name: CREATE MENU CATEGORY
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/menu/category"
  body:
    type: json
    data: |-
      {
        "name": "Minuman",
        "description": "Kategori minuman",
        "image_url": null
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
