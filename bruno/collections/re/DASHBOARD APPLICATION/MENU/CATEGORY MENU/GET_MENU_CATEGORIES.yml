info:
  name: GET MENU CATEGORIES
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/menu/category"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
