info:
  name: CREATE ORGANIZATION
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/organization"
  body:
    type: json
    data: |-
      {
        "name": "Bikasir Corp",
        "description": "Deskripsi organisasi"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
