info:
  name: SELECT ORGANIZATION
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{url}}/organization/:organizationId/select"
  params:
    - name: organizationId
      value: E9859BC314524256B9CEAB5BCBA42077
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
