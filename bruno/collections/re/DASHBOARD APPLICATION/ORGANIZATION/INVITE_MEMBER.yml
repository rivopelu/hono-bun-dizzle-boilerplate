info:
  name: INVITE MEMBER
  type: http
  seq: 4

http:
  method: POST
  url: "{{url}}/organization/member/invite"
  body:
    type: json
    data: |-
      {
        "email": "pelurivo@gmail.com",
        "role": "MEMBER",
        "merchant_ids": []
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
