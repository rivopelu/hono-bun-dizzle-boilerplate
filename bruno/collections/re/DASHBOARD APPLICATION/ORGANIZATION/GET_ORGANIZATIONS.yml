info:
  name: GET ORGANIZATIONS
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/organization"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
