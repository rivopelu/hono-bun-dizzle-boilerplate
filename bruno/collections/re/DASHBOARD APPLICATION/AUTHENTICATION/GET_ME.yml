info:
  name: GET ME
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/auth/me"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
