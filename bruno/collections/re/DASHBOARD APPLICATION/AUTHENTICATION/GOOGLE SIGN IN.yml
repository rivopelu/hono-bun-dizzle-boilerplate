info:
  name: GOOGLE SIGN IN
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/auth/google"
  body:
    type: json
    data: |-
      {
        "token": "eyJhbGciOiJSUzI1NiIsImtpZCI6..."
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
