info:
  name: SIGN UP
  type: http
  seq: 1

http:
  method: POST
  url: "{{URL}}/auth/sign-up"
  body:
    type: json
    data: |-
      {
          "name": "Test User",
          "email": "test2@example.com",
          "password": "password123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
